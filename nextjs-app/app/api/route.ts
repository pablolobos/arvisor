// Convert these to Route Handlers for better performance
export const runtime = 'edge' // Optional: Move to edge runtime

// Example for mail
export async function POST(request: Request) {
    // Your existing logic, but optimized
} 